"use client";

/** Subtle animated film grain — editorial craft feel (Monolith-adjacent). */
export function FilmGrain() {
  return (
    <div
      className="film-grain pointer-events-none fixed inset-0 z-[9998]"
      aria-hidden
    />
  );
}
